var greeting = "Hello, everybody!";
// the value of the greeting variable has to be "Hello, everybody!"

var specialGuest = "Neil deGrasse Tyson";

var greetSpecialGuest = "Hello, " + specialGuest + "!";
// we use + operation inside the single or double quotes to join the strings together as speicalGuest variable is string

var topic = "space";

var conversation = `Let's talk about ${topic}.`;
// we use placeholders inside the back tick to join the strings. ${topic} is "space"
